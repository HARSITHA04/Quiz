<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quiz Website</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #000; /* Black background */
      color: #fff; /* White text */
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      text-align: center;
      width: 90%;
      max-width: 500px;
    }
    .page {
      display: none;
    }
    .active {
      display: block;
    }
    button {
      padding: 10px 20px;
      margin: 10px;
      font-size: 16px;
      cursor: pointer;
      background-color: #007BFF;
      color: white;
      border: none;
      border-radius: 5px;
    }
    button:hover {
      background-color: #0056b3;
    }
    .options button {
      display: block;
      width: 100%;
      margin: 5px 0;
      background-color: #333;
      border: 1px solid #555;
    }
    .options button:hover {
      background-color: #444;
    }
    .options .selected {
      background-color: #007BFF;
      color: #fff;
    }
    .result.correct {
      color: green;
    }
    .result.incorrect {
      color: red;
    }
    input {
      padding: 10px;
      margin: 10px 0;
      width: 80%;
      max-width: 400px;
    }
    #timer {
      font-size: 18px;
      margin-bottom: 20px;
    }
    #questionNumber {
      margin-bottom: 10px;
      font-size: 20px;
      font-weight: bold;
    }
  </style>
</head>
<body>

<div class="container">

  <!-- Welcome Page -->
  <div id="welcomePage" class="page active">
    <h1>Welcome to the Quiz</h1>
    <form id="loginForm">
      <input type="email" id="email" placeholder="Enter your email" required><br>
      <input type="password" id="password" placeholder="Enter your password" required><br>
      <button type="submit">Start Quiz</button>
    </form>
  </div>

  <!-- Quiz Page -->
  <div id="quizPage" class="page">
    <div id="timer">Time Left: 10:00</div>
    <div id="questionNumber">Question 1 of 10</div>
    <h1 id="questionText">Question will appear here</h1>
    <div class="options" id="optionsContainer"></div>
    <div>
      <button id="prevButton">Previous</button>
      <button id="nextButton">Next</button>
    </div>
  </div>

  <!-- Results Page -->
  <div id="resultsPage" class="page">
    <h1>Quiz Results</h1>
    <div id="results"></div>
    <button id="thankButton">Thank You</button>
  </div>

  <!-- Thanking Page -->
  <div id="thanksPage" class="page">
    <h1>Thank You for Participating!</h1>
    <button id="homeButton">Return to Home</button>
  </div>

</div>

<script>
  const questions = [
    {
      question: "Which is the largest organ in the human body?",
      options: ["Liver", "Skin", "Heart", "Brain"],
      answer: "Skin"
    },
    {
      question: "What is the hardest natural substance on Earth?",
      options: ["Gold", "Iron", "Diamond", "Graphite"],
      answer: "Diamond"
    },
    {
      question: "Which planet is known as the Morning Star?",
      options: ["Mars", "Jupiter", "Venus", "Mercury"],
      answer: "Venus"
    },
    {
      question: "Who painted the ceiling of the Sistine Chapel?",
      options: ["Da Vinci", "Raphael", "Michelangelo", "Van Gogh"],
      answer: "Michelangelo"
    },
    {
      question: "What is the chemical symbol for gold?",
      options: ["Ag", "Au", "Fe", "Pb"],
      answer: "Au"
    },
    {
      question: "What is the smallest country in the world?",
      options: ["Monaco", "Malta", "Vatican City", "Liechtenstein"],
      answer: "Vatican City"
    },
    {
      question: "Who discovered penicillin?",
      options: ["Marie Curie", "Alexander Fleming", "Isaac Newton", "Albert Einstein"],
      answer: "Alexander Fleming"
    },
    {
      question: "What is the capital city of Australia?",
      options: ["Sydney", "Melbourne", "Canberra", "Perth"],
      answer: "Canberra"
    },
    {
      question: "What is the square root of 144?",
      options: ["10", "11", "12", "13"],
      answer: "12"
    },
    {
      question: "Which ocean is the deepest in the world?",
      options: ["Atlantic Ocean", "Indian Ocean", "Arctic Ocean", "Pacific Ocean"],
      answer: "Pacific Ocean"
    }
  ];

  let currentPage = 0;
  let currentQuestion = 0;
  let selectedAnswers = [];
  let timerInterval;

  const pages = document.querySelectorAll('.page');
  const loginForm = document.getElementById('loginForm');
  const questionText = document.getElementById('questionText');
  const questionNumber = document.getElementById('questionNumber');
  const optionsContainer = document.getElementById('optionsContainer');
  const prevButton = document.getElementById('prevButton');
  const nextButton = document.getElementById('nextButton');
  const resultsContainer = document.getElementById('results');
  const timerDisplay = document.getElementById('timer');

  loginForm.addEventListener('submit', (e) => {
    e.preventDefault();
    changePage(1);
    startTimer(600); // 10 minutes in seconds
    loadQuestion();
  });

  prevButton.addEventListener('click', () => {
    if (currentQuestion > 0) {
      currentQuestion--;
      loadQuestion();
    }
  });

  nextButton.addEventListener('click', () => {
    if (currentQuestion < questions.length - 1) {
      currentQuestion++;
      loadQuestion();
    } else {
      showResults();
    }
  });

  document.getElementById('thankButton').addEventListener('click', () => {
    changePage(3);
  });

  document.getElementById('homeButton').addEventListener('click', () => {
    changePage(0);
    clearInterval(timerInterval);
  });

  function changePage(pageIndex) {
    pages[currentPage].classList.remove('active');
    pages[pageIndex].classList.add('active');
    currentPage = pageIndex;
  }

  function loadQuestion() {
    const question = questions[currentQuestion];
    questionNumber.textContent = `Question ${currentQuestion + 1} of ${questions.length}`;
    questionText.textContent = question.question;
    optionsContainer.innerHTML = question.options
      .map((option) => {
        const selected = selectedAnswers[currentQuestion] === option ? 'selected' : '';
        return `<button class="option ${selected}" onclick="selectAnswer('${option}')">${option}</button>`;
      })
      .join('');
  }

  function selectAnswer(answer) {
    selectedAnswers[currentQuestion] = answer;
    loadQuestion(); // Refresh to update button styles
  }

  function showResults() {
    clearInterval(timerInterval);
    changePage(2);
    resultsContainer.innerHTML = questions.map((q, index) => {
      const correct = selectedAnswers[index] === q.answer;
      return `<p class="result ${correct ? 'correct' : 'incorrect'}">
        ${index + 1}. ${q.question} <br>
        Your Answer: ${selectedAnswers[index] || 'No Answer'} <br>
        ${!correct ? `Correct Answer: ${q.answer}` : ''}
      </p>`;
    }).join('');
  }

  function startTimer(duration) {
    let timeLeft = duration;
    timerInterval = setInterval(() => {
      const minutes = Math.floor(timeLeft / 60);
      const seconds = timeLeft % 60;
      timerDisplay.textContent = `Time Left: ${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
      timeLeft--;

      if (timeLeft < 0) {
        clearInterval(timerInterval);
        showResults();
      }
    }, 1000);
  }
</script>

</body>
</html>


